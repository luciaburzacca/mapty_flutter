import 'package:flutter/material.dart';

class RegistrazioneUtente extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Registrazione Utente'),
      ),
      body: Center(
        child: Text('Schermata di Registrazione Utente'),
      ),
    );
  }
}